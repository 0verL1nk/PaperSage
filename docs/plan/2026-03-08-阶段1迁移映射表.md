# 2026-03-08 阶段1迁移映射表

## 目标

记录 `utils/utils.py` 到新仓储层的迁移映射，避免后续继续向遗留模块扩张。

## 已迁移（第一批）

旧位置：`utils/utils.py`  
新位置：`agent/adapters/sqlite/project_repository.py`

1. `create_project_session`
2. `update_project_session`
3. `list_project_sessions`
4. `ensure_default_project_session`
5. `delete_project_session`
6. `list_project_session_messages`
7. `count_project_session_messages`
8. `list_project_session_messages_page`
9. `save_project_session_messages`
10. `create_project`
11. `list_projects`
12. `get_project_by_uid`
13. `update_project`
14. `add_file_to_project`
15. `remove_file_from_project`
16. `set_project_file_active`
17. `list_project_files`
18. `get_file_project_counts`
19. `ensure_default_project_for_user`
20. `ensure_projects_tables`

## 适配层变更

1. `agent/adapters/project_store.py` 已切换为从 `agent/adapters/sqlite/project_repository.py` 导入，不再直接依赖 `utils/utils.py`。

## 页面层变更（已完成）

1. `pages/1_file_center.py` 的项目相关能力已切换到 `agent/adapters/sqlite/project_repository.py`。
2. `pages/3_project_center.py` 的项目相关能力已切换到 `agent/adapters/sqlite/project_repository.py`。
3. `ui/agent_center_page.py` 的 `ensure_default_project_for_user` 已切换到新仓储路径。

## 测试层变更（已完成）

1. `tests/unit/test_projects_db.py` 项目仓储函数导入已切换到 `agent/adapters/sqlite/project_repository.py`。
2. `tests/unit/test_project_file_binding.py` 项目文件绑定函数导入已切换到新仓储路径。
3. `tests/unit/test_project_session_history.py` 会话仓储函数导入已切换到新仓储路径。
4. `tests/integration/test_streamlit_e2e.py` 绑定默认项目相关函数导入已切换到新仓储路径。

## 兼容层变更（已完成）

1. `utils/utils.py` 中上述项目/会话函数已改为懒加载转发到 `agent/adapters/sqlite/project_repository.py`。
2. 对外函数签名保持不变，避免调用方一次性破坏。

## 下一步建议

1. 为新仓储层补充更细粒度单测（例如 legacy schema 分支、异常路径）。
2. 推进阶段 3：继续拆分 `ui/agent_center_page.py`（controller/view/state）；当前已完成 controller/state 第一批拆分，并新增策略侧栏、历史面板与工具签名去重逻辑迁移。
3. 继续拆分用户设置仓储（`api_key/model/base_url/runtime_tuning`）到独立 sqlite repo，逐步移除 `agent/adapters/user_settings.py -> utils/utils.py` 的透传依赖。
